{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "node --max-old-space-size=256 resolve-failed-migrations.js && prisma migrate deploy && node --max-old-space-size=256 server.js"
  }
}
