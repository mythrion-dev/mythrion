FROM sonarqube:community

# SonarQube Community Edition — for Railway deployment
# Requires a PostgreSQL database (Railway plugin) and at least 2GB RAM.
# All configuration is via environment variables set in Railway dashboard.
