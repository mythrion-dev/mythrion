{
  "name": "mythrion",
  "version": "0.1.0",
  "private": true,
  "description": "Mythrion monorepo — root orchestrator for local development. Deploys are unchanged: Railway runs server/, Vercel runs client/.",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run start:dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "setup": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
